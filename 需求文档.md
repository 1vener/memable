# 本地媒体相似度/重复检测管理系统

## 产品需求文档（PRD）

版本：

```
v1.0
```

------

# 1. 项目概述

## 1.1 项目目标

开发一个运行在本地的视频和图片重复检测管理软件。

代码注释使用中文

支持：

- 本地媒体库管理
- 图片/视频扫描
- 媒体元数据采集
- 图片重复检测
- 视频重复检测
- 相似媒体分析
- 重复报告生成
- 文件整理归档

特点：

- 完全本地运行
- 不依赖云服务
- 支持大规模媒体库
- 支持后续AI扩展

------

# 2. 技术方案

## 技术栈

| 模块     | 技术           |
| -------- | -------------- |
| 客户端   | Flutter        |
| 服务端   | Go             |
| 数据库   | SQLite         |
| 视频解析 | ffprobe        |
| 视频处理 | ffmpeg         |
| 图片处理 | Go image + golang.org/x/image/bmp |
| HEIC/CR2 | ffmpeg 临时转码 |
| 任务调度 | Go Worker Pool |

------

# 3. 功能需求

# 3.1 收藏库管理

用户可以维护媒体收藏目录。

例如：

```
照片库

D:/Pictures


视频库

E:/Videos
```

支持：

- 添加目录
- 删除目录
- 修改目录
- 同步扫描（合并原“开始扫描”与“修复扫描”，见 3.2）

删除收藏库时，必须在同一数据库事务中删除收藏库记录、关联媒体记录和扫描会话；同时删除该库生成且已无其他收藏库引用的缩略图文件。收藏库删除时，关联的重复报告数据随 media 记录外键级联清理（见 7.2）。不得删除收藏库目录中的源媒体文件。

路径单独管理。

收藏库的 `path` 字段存储根目录绝对路径；库内所有媒体/缩略图均存储**相对该根目录的路径**（UTF-8 规范化）。

原因：

例如：

旧：

```
D:/Media
```

新：

```
E:/Media
```

只需 UPDATE `libraries.path` 一处即可，无需重新扫描、无需更新任何子文件记录；展示和搜索时拼接 `library.path || relative_path` 得到完整路径。

- 收藏库扫描后支持展示文件树，当删除文件或目录时同时删除数据库里相关数据和生成的缩略图/视频帧（后文会提到）

## 3.1.1 目录重命名与移动

收藏库文件树中的目录节点支持**重命名**与**移动到库内指定目录**（右键菜单），操作**同步执行**：先改本地磁盘，成功后批量更新 `media.relative_path` 前缀（`RenameDirectoryPrefix`，按目录边界精确匹配：整段相等或后随 `/`，不会误伤 `a/bc` 这类名称相似但目录不同的路径）；数据库更新失败时回滚磁盘，尽力保证一致。

规则：

- 根目录不可重命名/移动；名称不能包含 `/\:*?"<>|` 与 `.`/`..`。
- 目标位置已存在同名目录时返回 409，由用户更换目标或先改名，不做自动避让。
- 移动目标必须选择库内目录（目录树弹窗），禁止移动到自身或其子孙目录。
- 移动仅支持同卷（`os.Rename`）；跨卷（挂载点/符号链接场景）直接报错，不做递归复制。
- Windows 大小写改名（`A` → `a`）走"临时名中转"两步：直接 rename 在大小写不敏感文件系统上会失败或无效果；此时跳过目标存在性检查（新路径与源在文件系统上等价）。
- 目录内无数据库记录（未扫描的新文件）也能操作：仅移动磁盘，下次扫描自动入库；已有记录的元数据（缩略图/hash）随前缀更新保留，不重算。
- 重复报告按 `media_id` 引用成员，路径变化不影响分组，无需重建；目录对比报告的旧目录引用由用户重新生成。

------

# 3.2 媒体扫描（同步扫描）

将"开始扫描"和"修复扫描"合并为统一的"同步扫描"功能：一次扫描同时完成增量新增、修改检测、异常修复和缺失清理。

扫描收藏库时，以 `library_id + relative_path` 匹配本地文件与 `media` 记录，按以下规则处理：

- **跳过**：本地文件和数据库记录都存在，且 `mtime`、`file_size` 一致、处理状态完整、必要字段和缩略图文件正常时，跳过处理。
- **新增**：本地文件存在但数据库记录不存在时，作为新增文件进行完整扫描并写入数据库。
- **重新处理**：本地文件和数据库记录都存在，但文件属性发生变化（`mtime`/`file_size` 任一变化），或者数据库记录、hash、metadata、缩略图不完整时，重新处理并更新记录（视频需重新生成封面图）。
- **清理**：数据库记录存在但本地文件不存在时，在收藏库完整遍历成功后删除对应 `media` 记录、持久化缩略图以及失效的关联结果（重复报告数据随 media 外键级联清理，见 7.2）。不删除任何源媒体文件。
- **清理安全边界**：收藏库根目录不可访问、扫描被取消或遍历过程中发生 I/O 错误时，**不执行缺失文件清理**，避免因磁盘未挂载、移动硬盘断开、权限不足等误删数据库记录。
- **强制重新处理**：可选提供"强制重新处理全部文件"开关，用于彻底修复或算法升级后的重新计算，此时忽略跳过判定，对所有文件重新采集 metadata/hash/缩略图。

其中"处理状态完整、必要字段和缩略图文件正常"包含：`media` 处理状态为成功、metadata 与必要 hash 齐全、`thumbnail_path` 存在且缩略图文件在磁盘上真实存在。

扫描流程：

```
扫描目录（校验根目录可访问）

↓

遍历本地文件

↓

按 (library_id, relative_path) 匹配 media 记录，判定跳过 / 新增 / 重新处理

↓ （新增或重新处理时）

判断类型并采集metadata

↓

生成hash

↓

生成缩略图

↓

保存数据库

↓ （仅在遍历完整成功后）

清理本地已不存在的 media 记录及其缩略图/失效关联结果
```

### 扫描进度与速度估算

后台任务显示以下进度信息：

- **已处理 / 总数**：`processed_items / total_items`
- **处理速度**：双口径 EWMA（指数加权移动平均）估算——**字节/秒**（`instant = 字节增量 / 间隔`，`ewmaRate = 0.25 × instant + 0.75 × previous`）与**文件/秒**（已完成工作文件数增量，同样 EWMA 平滑）；跳过文件不参与速率计算，避免被快速跳过拉高速率
- **预计剩余**：综合剩余字节与剩余文件数：`ETA = max(剩余工作字节 / 字节速率, 剩余工作文件数 / 文件速率)`。剩余工作字节 = 总工作字节 − 已处理字节（均不含跳过文件）；剩余工作文件数 = 剩余文件数 ×（1 − 已见跳过比例）。取较大值（保守）避免"快完了"误报，并做轻量平滑（新值占 30%）防止 ETA 跳变
- 速度为 0（如正在处理耗时文件）时不显示 ETA，避免显示无限大
- 进度写库节流：最少 500ms 间隔，避免每个文件 UPDATE SQLite 成为扫描瓶颈
- 任务结果摘要包含 `total_bytes` / `processed_bytes`（已处理总文件大小）
- 扫描处理每个文件时打印其所在目录与文件名（日志），便于定位卡住/失败的文件
- 增量/完整性判定使用内存快照：扫描前一次性加载该库全部媒体记录，逐文件判定不再串行查询 SQLite
- 媒体入库使用单条 SQL（`INSERT ... ON CONFLICT ... RETURNING id`），避免写完再查一次

------

# 3.3 媒体格式支持

## 格式分类与解码策略

| 格式 | 类型 | 解码方式 | 说明 |
| ---- | ---- | -------- | ---- |
| .jpg .jpeg .jfif | 图片 | Go 原生 | 标准库 image/jpeg |
| .png | 图片 | Go 原生 | 标准库 image/png |
| .bmp | 图片 | Go 原生 | golang.org/x/image/bmp |
| .gif | 图片 | **跳过** | 按产品要求跳过，不计算 SHA1、不生成缩略图、不写入 media 表 |
| .heic | 图片 | FFmpeg 转码 | 依赖 FFmpeg HEVC 解码器；转临时 PNG 后走统一解码流程 |
| .cr2 | 图片 | FFmpeg 转码 | 依赖 FFmpeg RAW 解码能力；不支持时单文件失败不中断扫描 |
| .mp4 .mov .avi .mkv | 视频 | FFmpeg | ffprobe 采集 metadata + ffmpeg 抽帧/sprite |
| .mpg .mpeg .m4v .webm .wmv .flv | 视频 | FFmpeg | 同上 |
| .3gp .ts | 视频 | FFmpeg | TS 时长可能不准确，按多级回退探测 |

## GIF 跳过策略

- 扫描遍历时识别 `.gif` 扩展名
- 记录为 known-but-skipped（`WalkResult.SkippedGIF` 计数）
- 不计算 SHA1、不生成缩略图、不写入 media 表
- 未来若开启 GIF 支持：仅读取第 0 帧作为缩略图和感知哈希，不落盘所有动画帧

## HEIC / CR2 依赖条件

- 依赖 FFmpeg 的 HEVC（HEIC）和 RAW（CR2）解码器
- 转换流程：`ffmpeg -v error -y -i input.heic -frames:v 1 output.png` → Go image/png 解码
- 单文件超时 60 秒（`context.WithTimeout`）
- 临时目录使用 `os.MkdirTemp` + `defer os.RemoveAll`，不使用源文件名避免并发冲突
- FFmpeg 解码失败时：增加 `failed_items`，记录完整路径和 stderr，任务继续处理后续文件，不写入不完整的 media 记录
- HEIC/CR2 最终支持能力取决于 FFmpeg 构建版本，通过 `/api/health` 的 `ffmpeg.heic_decode` / `ffmpeg.cr2_decode` 字段报告

## 统一图片解码

所有图片格式通过 `DecodeImage(ctx, path, decoder)` 统一入口解码，一次解码后复用 `image.Image`：

```
DecodeImage（Go 原生 或 FFmpeg 临时转换）
    ↓
获取宽高和格式（metadata）
    ↓
ImagePerceptualHashesFromImage（pHash/dHash/aHash）
    ↓
GenerateThumbnailFromImage（缩略图）
```

避免大图片被重复完整解码两到三遍。

- 超过 2400 万像素的超大图片（高像素相机照片、拼接全景图等）不直接全分辨率解码：解码前先读取头部尺寸，超过阈值时经 FFmpeg `scale` 滤镜限分辨率转码（最长边 3000px、保持宽高比）后再解码，避免解码阶段一次性分配数百 MB 内存导致进程 OOM（32 位构建堆上限约 2GB）；FFmpeg 不可用或转码失败时不回退 Go 原生全分辨率解码，仅将该文件记为失败并继续扫描。

## TS 时长探测

MPEG-TS 文件的 `format.duration` 可能为空或不准确，按以下顺序获取时长：

1. `format.duration`
2. 视频流的 `duration`
3. `duration_ts × time_base`
4. 都不存在时标记为未知时长（封面回退到 0 秒，sprite pHash 记录警告，不崩溃）

------

# 4. 图片处理需求

## 4.1 图片metadata

采集：

| 字段     |
| -------- |
| 文件大小 |
| 格式     |
| 宽度     |
| 高度     |
| 创建时间 |
| 修改时间 |
| 文件路径 |

------

## 4.2 图片hash

生成：

```
SHA1
```

用途：

完全相同文件检测。

生成：

```
pHash
dHash
aHash
```

用途：

相似图片检测。

------

## 4.3 图片缩略图

生成：

```
最大边400px（thumbnail.max_edge 可配置）
JPEG 编码（quality 90），CatmullRom 高质量缩放
```

保存：

```
例子：thumbnail/image/
路径可在配置文件中配置
```

- 缩略图根目录默认使用系统数据目录（Windows `%LOCALAPPDATA%`、macOS `~/Library/Application Support`、Linux `$XDG_DATA_HOME` 或 `~/.local/share` 下的 `memable/thumbnails/image`）；配置文件设置了 `thumbnail.image_dir` 时使用配置值。旧版默认在系统缓存目录（macOS `~/Library/Caches`、Linux `$XDG_CACHE_HOME`），升级后由用户手动迁移目录或直接配置指向旧位置。
- `media.thumbnail_path` 只存相对该根目录的内容寻址路径（`{哈希前缀}/{哈希}.jpg`），不包含根目录；更换或整体移动缩略图目录时只需修改配置并迁移目录内容，无需修改数据库。
- 缩略图采用 JPEG q90（无透明通道，含透明像素的图先合成到白底），文件体积约为旧版 PNG 无损编码的 1/6~1/8；缩放使用 CatmullRom 高质量滤波，大图缩小比最近邻更清晰。
- 旧版本缩略图为 `.png` 后缀：完整性扫描时以 `.png` 结尾的记录视为旧格式重新生成（`.jpg`），被替换的旧文件在无其他媒体引用后自动删除。

------

# 5. 视频处理需求

# 5.1 视频metadata

采集：

| 字段        |
| ----------- |
| size        |
| duration    |
| video_codec |
| audio_codec |
| format      |
| width       |
| height      |
| frame_rate  |
| bit_rate    |
| 文件路径    |

------

# 5.2 视频SHA1

生成：

- 主扫描（scan/repair）**不生成**视频 SHA1，避免每个大视频多一次全文件读取。
- 需要时由独立后台任务 `scan_sha1`（补齐 SHA1）按收藏库触发：只处理该库 `sha1 IS NULL` 的记录（实际主要是视频，兼容历史缺 sha1 的图片），不强制重算，支持取消与进度统计。
- 主扫描重新处理某个视频时会清空其旧 SHA1（内容已变，旧值无效），需要时可再次运行补齐任务。

用途：

- 重复报告中的 SHA1 完全相同分组（`sha1_exact`），仅对已补齐 SHA1 的记录生效。
- 搜索框的 SHA1 精确搜索。

例如 `movie.mp4` 与 `movie_copy.mp4` 文件名不同但 SHA1 一致，补齐后即被识别为完全相同视频。

------

# 5.3 视频封面图缩略图

视频只持久化一张封面图缩略图。sprite 生成流程同时产出 25 帧拼接图的 pHash、封面帧的 pHash 和封面：优先从中间帧开始向两侧选择非黑屏/非纯色帧作为封面，避免再次启动 FFmpeg 读取视频；若 sprite 生成或封面选择失败，该文件记录失败并继续扫描。用于 sprite pHash 的截图和拼接图仅在计算过程中临时生成，计算完成后立即删除。封面帧的 pHash（`media.cover_phash`）与 sprite pHash 一并计算：sprite pHash 供视频重复检测，封面帧 pHash 供以图搜图对比视频缩略图（见 6.1）。

## 封面截取时间点

先通过 `ffprobe` 获取视频 duration，再按以下规则选择时间点：

```
duration < 10s       -> 50% duration
10s <= duration < 60s -> 30% duration
duration >= 60s      -> 10% duration
```

如果选定时间点抽帧失败、画面为黑屏或接近纯色，则在该时间点前后按 10% duration 间隔重试，最多尝试 5 个时间点；仍失败时使用 50% duration，最后才回退到 0s。

封面生成流程：

```
ffprobe 获取 duration

↓

计算封面时间点

↓

ffmpeg 抽取单张图片

↓

过滤黑屏/近纯色并 resize

↓

保存到配置的 video thumbnail 目录
```

缩略图最大边为 400px（`thumbnail.max_edge` 可配置，JPEG q90 + CatmullRom 缩放，规则同 4.3），生成路径登记到 `media.thumbnail_path`。

- 视频封面根目录默认使用系统推荐目录（`memable/thumbnails/video`，规则同 4.3）；配置文件设置了 `thumbnail.video_dir` 时使用配置值。
- `media.thumbnail_path` 只存相对该根目录的内容寻址路径，缩略图目录整体移动后仅需更新配置，无需改库。
- 视频封面内容寻址 key 由 `oshash + file_size + duration_ms` 派生（主扫描不计算视频 SHA1；之后补齐 SHA1 也不会改变已生成封面的 key）；图片缩略图仍由 SHA1 派生。

------

# 5.4 视频相似度检测

## 第一层

SHA1：

```
完全重复
```

结果：

100%重复。

仅对已补齐 SHA1 的视频记录生效。时长小于 4 秒的视频适用短视频专用规则：SHA1 或 OSHash 任一相同即可进入相似分组，不再依赖 sprite pHash；普通视频未补齐 SHA1 时由后续 sprite pHash 相似层覆盖。

------

## 第二步：oshash 粗筛

使用 Stash/OpenSubtitles OSHash（字段名统一使用 `oshash`）做文件级快速指纹粗筛。

说明：

- oshash 相同可快速定位相同或高度可疑候选。
- oshash 是文件级快速指纹，不是视觉相似哈希，不能单独判定转码、裁剪、加水印等视觉相似视频。
- oshash 不同的视频也可以进入后续视觉相似检测，避免漏掉转码后视觉相似但文件头尾不同的内容。
- **短视频例外**：当视频时长 `<4000ms` 时，不再执行 sprite pHash 比较；SHA1 或 OSHash 任一相同即可判定为短视频相似，SHA1/OSHash 关系按 OR 语义传递合并。

------

## 第三步：视觉相似检测

参考 Stash `videophash` 流程，为每个视频生成一个整体视觉 sprite pHash：

```
分段快速抽帧：把 5% 到 95% 区间等分为 5 段，每段用 -ss 快速定位到段中点，
只解码约 1 秒窗口并抽 5 帧（fps=5, tile=1×5），共 25 帧

↓

每张截图缩放为宽 160px；5 段横条按行拼成 5 × 5 sprite

↓

封面优先取中间帧，向两侧寻找有效帧

↓

对 sprite 图生成 64 bit pHash；pHash 的二维 DCT 使用可分离的一维变换，复杂度由 O(n^4) 降为 O(n^3)
```

截图时间点（每段取段中点附近 1 秒窗口，fps=5 均匀取 5 帧）：

```
seg_count = 5
seg_span = duration * 0.90 / seg_count
seg_mid_i = duration * 0.05 + i * seg_span + seg_span / 2, i = 0..4
ffmpeg -ss (seg_mid_i - 0.5) -i video -t 1 -vf "fps=5,scale=160:-1,tile=5x1"
```

分段抽帧相比整段 `trim+fps` 解码，长视频解码量从约 90% 时长降到约 5 秒（实测 10 分钟 720p 视频 sprite 从约 10 秒降到约 0.7 秒）；分段失败时依次回退单条 `trim+fps+tile` 整段抽帧、逐帧 25 次抽帧。

这 25 张截图和 sprite 图只作为计算过程中的临时文件或内存数据，计算完成后立即删除，也不作为缩略图保存。视频持久化图片仍只有一张封面图。

视频 sprite pHash 保存到：

```
media.phash
```

比较两个视频的 sprite pHash：

```
video_phash_distance = Hamming(media_a.phash, media_b.phash)
```

同时使用视频时长差作为辅助条件：

```
duration_diff = abs(duration_a - duration_b)
```

对于时长 `>=4000ms` 的普通视频，只有 `video_phash_distance` 和 `duration_diff` 均满足配置文件阈值时，才判定为可能视觉相似。短视频不进入该 sprite pHash 比较流程。

命中的相邻视频按连通关系合并为重复/相似分组，而不是只输出两两结果。

------

# 5.5 OSHash

参考：

Stash项目的 OpenSubtitles OSHash 实现。

算法：

```
result = file_size
       + uint64_le_sum(first 64KiB)
       + uint64_le_sum(last 64KiB)
```

输出：

```
16位十六进制字符串
```

说明：

该 oshash 是文件级快速指纹，不是视觉相似哈希；用于粗筛候选并兼容 Stash/OpenSubtitles 生态。视觉相似以第三步的 sprite pHash 为准。

用途：

视频整体指纹。

保存：

```
media.oshash
```

用于：

快速过滤候选视频。

检测流程：

```
oshash粗筛

↓

Stash风格视频sprite pHash候选分组
```



# 6.搜索

## 6.1搜索框

> - 搜索框支持输入文字搜索，搜索类型包含文件名（可以模糊匹配全路径）,sha1。
>
> 搜索结果显示文件全路径、图片缩略图或视频封面图等
>
> - 搜索框支持图片搜索，对比图片缩略图和视频封面图
>
> 实现细节：以图搜图用查询图片的 pHash 与媒体记录比对（Hamming 距离 ≤ 阈值）。图片对比 `media.phash`（全图 pHash，与 400px 缩略图 pHash 因算法内部统一缩放而等价）；**视频对比 `media.cover_phash`（封面帧/缩略图的 pHash，v7 新增列），绝不能用 `media.phash`（视频的 phash 是 25 帧 sprite 拼贴图 pHash，与单帧查询图不可比）**。存量视频的 `cover_phash` 由 `needsSync` 在下次完整性/修复扫描时自动补齐（重新抽 sprite 并写入）；补齐前该视频不参与以图搜图。
>
> - 搜索框支持**以视频搜视频**：选择本地视频（不限制大小），服务端按扫描同口径提取视频 sprite pHash 与首帧 pHash，**省略 oshash 对比**。
>
> 实现细节：以视频搜视频上传查询视频（`POST /api/search/video/upload`，multipart 字段 `video` + 两个距离阈值 `image_max_distance`/`video_max_distance`），提取 sprite pHash 与库中**所有视频**的 `media.phash`（sprite pHash）比对（**不区分时长差、不区分 <4000ms 短视频**，与视频重复检测口径不同）；提取首帧 pHash 与库中**图片**的 `media.phash` 比对（不对比视频封面）。图片与视频两路距离阈值独立、由前端用户选择（0~64 整数，Slider 步进 1；默认图片 12、视频 16），相似度 = 1 − 距离/64。结果合并按距离升序。



## 6.2 选择本地文件目录，参照3.2媒体扫描，但结果写入同一套表，用 `scan_sessions.is_temporary=1` 标记为临时扫描，并关联一个唯一的 `scan_session_id`。图片缩略图和视频封面图放入临时目录；视频 sprite pHash 的过程截图和 sprite 图仅临时使用，计算后立即删除。

采用"同表 + 临时标记"而非物理临时表，原因：
- 避免临时表与正式表之间 JOIN 不可见导致的漏检
- 用户确认入库时只需 `UPDATE scan_sessions SET is_temporary=0` 并移动文件，无需跨表复制记录

扫描完进行相似检测，结果在应用内展示，不生成 HTML 文件。结果必须关联源文件，并显示文件完整路径、图片缩略图或视频封面图等。

搜索任务结束，用户可以选择是否入收藏库：选择"是"时，将文件**移动**到收藏库指定目录（不要复制），然后将对应 `scan_session_id` 下记录 `is_temporary` 置 0，临时目录的图片缩略图和视频封面图移动到配置的缩略图目录。

### 临时扫描库与入库

- 临时扫描会创建一个独立临时库（命名"临时扫描-xxx"），库列表接口返回 `is_temporary` 标记，客户端以"临时"徽标区分；临时数据**不参与重复报告检测**（报告查询排除 `is_temporary=1` 会话的媒体），但**参与搜索**。
- 临时库右键提供"移动到收藏库…"：选择目标正式收藏库与目标子目录（目录树选择，空=库根）后，将本地文件**移动**到目标库对应目录，并**同时更新 media 的 `library_id` 与 `relative_path`**（加上目标子目录前缀）；随后会话标记为已入库、删除临时库记录。
- **路径冲突处理**：目标路径为"目标目录/临时库最后一级目录名/原相对路径"（如临时库 `D:\tmp\test` 移到目标目录 `2026` → `D:\本地\2026\test\...`）；目标库已存在相同相对路径的媒体时，以"临时库最后一级目录名 + (1)"作为子目录段递增避让（如 `photos/我的下载(1)/a.jpg`），确定无冲突后再移动文件并更新记录。
- 接口：`POST /api/libraries/{id}/promote`（body：`target_library_id`、`target_dir`）。

## 6.3 收藏库文件搜索（工具栏，参照 Windows 10 文件搜索）

- 顶部工具栏在收藏库页（导航第 1 项）显示圆角搜索框（Windows 10 风格：搜索图标前缀、加载/清除后缀），输入防抖 350ms 自动搜索，回车立即搜索；搜索范围为**全部正式收藏库**（排除临时扫描库）。
- 结果以**目录级列表**显示在收藏库详情页右侧面板（替换文件网格视图），行内为目录名 + 相对路径·库名 + 命中文件数角标，命中文字大小写不敏感**高亮**；接口 `GET /api/libraries/search?q=`，返回目录命中与文件命中两类条目。
- 聚合语义（与 Windows 搜索一致）：**目录名命中 → 直接返回该目录**（`match_type=dir`）；**文件名命中 → 汇总到父目录，只返回父目录**（`match_type=file`，`match_count`=命中文件数，同一目录多文件命中去重为一行）；同一目录若同时命中目录名与文件名，以 `dir` 类型展示。命中判定逐段比较 `relative_path` 各路径段，根级文件命中汇总为库根（空路径）。
- **点击结果**：自动切换收藏库（`_libraries` 定位结果所属库）→ 退出搜索模式 → 目录树逐层懒加载展开祖先链并选中命中目录（`_FileTreePanelState.revealDir`），右侧文件网格同步加载该目录媒体。

# 7. 重复报告

扫描本地收藏库的重复数据，图片和视频分别统计，用户可单独查看图片、视频或全部结果。

重复统计结果在应用内展示，不生成 HTML 报告文件。报告数据存储在独立的重复报告表中（见 7.2），`background_tasks.result_json` 不再存放分组明细，仅保留任务进度与统计摘要（组数、文件数、各类型计数）。

报告生成任务在**独立任务队列**中串行执行：生成报告类任务（`report_duplicate` 及旧版 `report_image`/`report_video`）仅在报告队列内互相排队，与扫描、补齐 SHA1 等其他后台任务互不阻塞，扫描进行中仍可立即生成报告，反之亦然；排队位置也按各自队列分别计算。

## 7.1 报告生成选项

生成重复报告时可配置以下选项，选项仅影响本次生成，不修改配置文件与数据库中的阈值：

- **类型范围**：图片 / 视频 / 全部。
- **比较范围**：
  - **全部数据（默认）**：跨目录比较，重复组可包含不同目录下的文件。
  - **仅同一目录**：只比较同一目录直属文件（不包含子目录），重复组仅由同一目录内的文件构成。
- **图片选项**：相似度阈值 0–100（默认 90）。内部换算为 pHash Hamming 距离上限：`floor(64 × (100 − 阈值) / 100)`（90 → 距离 ≤ 6，100 → 0，0 → 64）。
- **视频选项**（四项）：
  - sprite pHash Hamming 距离阈值：默认取配置文件值，未配置时兜底为 12；
  - 允许时长差阈值：默认取两者中较大时长的 10%；
  - oshash 粗筛开关（默认开）：仅作为候选预分组加速，不作硬过滤，不同 oshash 的视频仍可进入视觉比较；
  - 包含 SHA1 完全相同结果开关（默认开）：关闭时报告中不包含 SHA1 完全相同的重复组。

## 7.2 重复报告数据模型与同步刷新

报告数据存入三张表：`duplicate_reports`（报告主表）、`duplicate_groups`（重复组）、`duplicate_group_members`（组成员），表结构见第 8 节。

- 组成员只存 `media_id` 引用，不冗余路径；目录、完整路径、缩略图等信息通过 JOIN `media` 实时取得，收藏库路径迁移后展示自动正确。
- 每次生成报告在单事务内替换旧报告：删除旧报告并级联删除其组与成员，写入新报告；生成成功时 `stale=0`，失败回滚且旧报告保持不变。每次只保留一份当前报告。
- **删除类变更**（媒体文件/目录删除、收藏库删除、同步扫描清理缺失文件）：统一走 media 删除入口，同一事务内删除 media 记录 → 外键级联删除成员记录 → 清理成员数 <2 的组 → 更新报告统计；收藏库删除时其下 media 全部删除，重复表随之清空。
- **重新处理 / 新增变更**：media 被重新处理时先移除其全部成员关系并清理 <2 的组，新增文件同样处理；随后将当前报告 `stale` 置 1。客户端打开报告时若 `stale=1`，显示"数据已变化，建议重新生成"提示。

### 7.2.1 检测性能（百万级）

- **pHash 相似度使用 MIH（Multi-Index Hashing）索引**：64-bit pHash 拆 4 段 × 16 bit，由鸽笼原理保证——两哈希 Hamming 距离 ≤ d 时必有一段距离 ≤ ⌊d/4⌋，查询只需枚举每段的邻居桶，**理论上无漏检**，复杂度从 O(n²) 降至 O(n·k + 候选数)；pHash 在检测前预解析为 uint64，用 `bits.OnesCount64(a^b)` 快速比较，不再循环内解析十六进制字符串。
- **视频先按时长分桶**：桶宽 = 允许时长差，只比较同桶与相邻桶的视频，进一步缩小候选；短视频（<4000ms）仍按 SHA1/OSHash 规则，不参与 pHash。
- **增量检测**：生成报告时若与上次报告的参数（scope、media_type、图片阈值、视频 pHash 距离、时长差）完全一致**且上次报告存在分组**，则只对 `media.created_at >= 上次报告时间` 的新增/修改媒体发起检测（索引仍含全量媒体，保证新文件可匹配存量），并与旧报告分组合并，大幅缩短重复生成耗时；任一阈值或范围变化、或旧报告为空（历史异常产物）时全量重建，防止空报告传染。媒体 Upsert 冲突时刷新 `created_at`，使重新处理过的文件可被增量识别。
- **报告写入**：组与成员使用 prepared statement 批量插入；写入前显式逐表清空旧报告（组、成员、报告），不依赖外键级联，避免历史外键未启用时残留孤儿数据；生成任务过程上报进度（加载/检测阶段），任务页可见。

## 7.3 仅同一目录模式

比较范围为"仅同一目录"时生成的报告，重复组仅由同一目录（不含子目录）直属文件构成；不同目录的文件即使重复也不成组。

### 7.3.1 目录树展示页

- 树仅构建包含重复文件的目录；树面板顶部提供目录搜索框，输入关键词实时过滤树节点（保留祖先链、匹配节点高亮并自动选中定位），清空后恢复完整树与折叠状态；选择目录后，右侧显示该目录直属的重复文件缩略图。
- 缩略图卡片使用 card_swiper 叠卡效果：重复的多张缩略图错位重叠、露出边缘，宽度可大于普通缩略图；hover 时轻量展开，点击后放大展示该组全部重复缩略图。
- 右键菜单支持"打开文件路径"（在文件管理器中打开所在位置，并选中该文件，方便一眼定位）、"排除重复"（人工判定该文件无重复，将其从当前报告中移除；仅对当前报告生效，重新生成报告后重新参与检测）。
- 点击卡片展开组内全部缩略图后，可对某一缩略图右键选择"删除此文件外该目录下所有重复文件"：保留被点击文件，删除组内其余文件（含缩略图、media 记录、本地源文件）。
- 成员缩略图右键菜单提供"删除此文件"：独立确认后删除该单个文件（同步删除本地文件、media 记录、缩略图）；详情弹窗内缩略图 hover 时显示完整文件路径。
- 目录详情页提供"删除此目录下所有重复数据"对话框：保留条件单选（最大文件 / 最小文件 / 最大修改时间 / 最小修改时间 / 最长文件名 / 最短文件名），带确定和取消按钮；确定后仅删除当前目录下的重复数据：组内本目录成员 ≥2（本目录内互相重复）按保留条件保留 1 个、删除其余本目录成员；组内本目录成员仅 1 个（只与其他目录文件重复）直接删除本目录这一份；其它目录的文件一律不动。

### 7.3.2 分组展示页

- 组按目录分组展示，分页显示；页大小可选（10 / 20 / 50 / 100），默认一页 20 组。
- 操作栏支持"一键清除本页重复文件"：保留条件单选（同上），带确定/取消；操作完成后更新当前分页数据。
- 缩略图卡片同 7.3.1 的 card_swiper 叠卡效果，点击展开组内全部缩略图，展开后可右键删除该文件外该目录下所有重复文件。
- 删除后刷新数据：某组不再有重复文件时移除该组，重复组总数减少（页数可能减少）；当前页越界时回退到最后一页。

## 7.4 全部数据模式

比较范围为"全部数据"（默认）时生成的报告，重复组可跨目录。

### 7.4.1 目录树展示页

- 树仅构建包含重复文件的收藏库目录和子目录；树面板顶部提供目录搜索框（同 7.3.1）；选择目录后，右侧显示该目录直属的重复文件缩略图。
- 缩略图右键菜单包含：打开文件路径（打开所在目录并选中该文件）、复制文件路径、打开图片/视频、排除重复（从当前报告移除，仅当前报告生效）、删除此文件（独立确认后删除单个文件，同步删除本地文件/media 记录/缩略图）。
- 菜单中另列出该组内当前目录之外的所有文件路径（一个路径一个菜单项），每个路径下有二级菜单：打开文件路径、复制文件路径、打开图片/视频、排除重复。
- 叠卡/缩略图点击进入组详情弹窗后，成员缩略图同样支持右键"删除此文件"，且 hover 时显示完整文件路径。

### 7.4.2 分组展示页

- 按重复组跨目录分组展示，分页显示；页大小可选（10 / 20 / 50 / 100），默认一页 20 组。
- 操作栏支持"一键清除本页重复文件"：保留条件单选（最大文件 / 最小文件 / 最大修改时间 / 最小修改时间 / 最长文件名 / 最短文件名），带确定/取消；操作完成后更新分页数据。
- 缩略图右键菜单包含：打开文件路径（打开所在目录并选中该文件）、复制文件路径、打开图片/视频、排除重复、删除此文件（独立确认后删除单个文件，同步删除本地文件/media 记录/缩略图）；本模式不做"删除此文件外本组重复文件"。

## 7.5 删除操作通用语义

- 删除范围：`media` 记录 + 持久化缩略图文件 + 本地源文件；删除后同步更新重复报告表（见 7.2）。
- 源媒体文件默认移入系统回收站（Windows 调用回收站 API，可恢复）；设置中提供"永久删除"选项。生成的缩略图与数据库记录直接删除。回收站不可用时提示并中止删除。
- 删除仅允许收藏库根目录内且存在于报告中的文件，越界路径拒绝。
- 危险操作前弹出确认对话框，写明影响范围（如"将删除 n 个文件，共 x GB，每组保留 1 个"），带确定/取消按钮。
- 删除成功后刷新当前视图：目录树保留其他节点展开/收起状态（不重建展开集合）；分页视图移除空组、更新组数与页数，当前页越界时回退到最后一页。

## 7.7 目录对比报告（所选目录 vs 存量数据）

- **独立页面**（侧边栏"目录对比"入口，与重复报告页分离）：页内提供"开始目录对比/重新生成"按钮，弹窗内选择收藏库、在该库目录树中选择目录（含子目录），并配置与重复报告相同的阈值参数（媒体类型、图片相似度、视频 pHash 距离、时长差）。
- 检测范围：**所选目录（含子目录）内的文件 vs 其余全部存量数据**；目标内部、存量内部均不比较，只产生"目标 vs 存量"的重复组。SHA1/OSHash/pHash 分组必须同时包含目标与存量成员。
- 结果存储于**独立三表** `dir_duplicate_reports/groups/members`（成员带 `is_target` 标记区分所选目录文件），**不替换重复报告**；重新生成时替换自身（只保留最新一份）。
- **结果展示与操作与重复报告页完全一致**：目录树（含顶部搜索框，实时过滤/高亮/自动定位）+ 重复分组双视图切换 + 媒体类型过滤（全部/图片/视频）+ 分页（10/20/50/100 组/页）+ card_swiper 叠卡 + 组详情弹窗（成员缩略图 hover 显示完整路径）；成员/叠卡右键菜单：打开文件路径、复制路径、打开媒体、**排除重复**（仅当前报告生效）、**删除此文件**（独立确认后删除单个文件，同步删除本地文件/media 记录/缩略图）、其它目录二级菜单、**删除此文件外本组重复文件**（目录树视图为"删除此目录外本组重复文件"）；工具栏提供**删除此目录下所有重复数据**（目录，语义与重复报告页一致：按"本目录成员"为单位处理、只删本目录数据——组内本目录成员 ≥2 按保留条件保留 1 个删其余，==1 直接删除本目录这一份，其它目录成员一律不动）与**一键清除本页**（本页 + 六种保留条件）；所选目录文件带"所选目录"角标。目录树语义与重复报告不同：目录对比的组是"目标 vs 存量"，同一目录内通常只有 1 个成员，因此**目录内存在重复组成员（≥1 个）即计入树**（file_count=该目录内成员数）；目录树叠卡展示**整组成员**（本目录成员优先、其它目录成员补充，数量徽标与叠卡反映整组重复文件总数，点击进组详情看全部成员）。
- 任务 kind=`report_directory`，进入报告队列执行；生成过程上报进度，页面轮询任务状态并在完成后自动刷新；进入页面时自动恢复对进行中任务的轮询。
- 接口：`POST /api/reports/directory-compare`（生成）、`GET /api/reports/directory-compare`（摘要）、`GET /api/reports/directory-compare/groups`（分组分页，支持 `kind` 过滤）、`GET /api/reports/directory-compare/tree`（目录树，支持 `kind` 过滤）、`POST /api/reports/directory-compare/exclude`（排除重复）、`POST /api/reports/directory-compare/clear`（一键清除：scope=directory/page/group + 六种保留条件）。
- 删除类变更（排除、清除、媒体删除、目录删除）后目录对比报告同步维护：成员数 <2 的组清理、`total_groups/total_files` 统计刷新（`Dir.PruneGroupsAndUpdateStats`，随 `PruneAfterMediaChange` 一并执行）。

## 7.6 UI/UX 设计规范

### 7.6.1 视觉风格

- Material 3 定制主题：圆角 12–16px、柔和阴影、细边框；中性色打底 + 单一强调色，语义色仅用于状态（重复、危险操作）。
- 路径、hash 使用等宽字体；文件大小、时长等数字使用等宽对齐。
- 默认浅色主题；设置中可切换深色 / 跟随系统。

### 7.6.2 自定义主题颜色

- 设置页提供主题色自定义：预设色板（若干常用强调色）+ 自定义取色器（支持 HEX / RGB 输入）。
- 自定义主题色可独立作用于浅色与深色模式；选择即时生效并本地持久化，重启后保留。
- 自定义主题色通过 Material 3 `ColorScheme` 派生页面背景、导航表面、卡片和输入区域颜色，并保持不同表面层级的中性色对比。
- 自定义主题色需满足对比度要求，保证可读性。

### 7.6.3 布局

- 左侧导航新增“首页”，保留“收藏库”管理入口。首页的“库 / 视频 / 照片 / 统计”使用顶部 Toolbar 内的紧凑分段导航，不在内容区新增独立 TabBar；窄窗口只显示功能图标，避免 Toolbar 溢出。
- “库”功能汇总全部正式收藏库，排除临时扫描数据。按“显示 → 库 → 分组层级”设置的前 N 级相对目录分组，默认 3 级；目录不足 N 级时按实际目录分组，根目录文件归入根目录组。目录组按组内最新 `media.mtime` 倒序，组内递归显示该目录及全部子目录媒体，并按 `mtime` 倒序。不再显示右侧时间刻度。
- “库”功能支持两种加载方式（工具栏切换，默认翻页）：**翻页模式**按页码分页（每页分组数可配置 10/20/50/100，默认 20，持久化 `ui.home.library_group_page_size`），底部紧凑分页栏（页码输入、上一页/下一页、总组数）；**懒加载模式**保留原触底懒加载（滚动到接近底部自动追加下一批分组）。加载方式持久化 `ui.home.library_load_mode`。
- “库”功能支持文件名搜索（工具栏搜索框，防抖 350ms + 回车立即）：命中文件所属目录分组**整组返回**（组内全部后代媒体），命中多个分组同时返回；组内命中的文件以主色描边高亮；搜索时同样支持翻页；无匹配显示空状态；清空恢复全部分组。后端 `GET /api/media/groups?q=` 实现搜索（大小写不敏感、排除临时媒体）。
- “库”功能在页面顶部显示设置条提供布局切换与缩放：网格自适应（按窗口宽度自动调整列数，保留完整画面）、网格正切（统一正方形单元，图片裁剪填充不留空隙）、瀑布流（按媒体原始宽高比排列）、自适应（按原始宽高比横向铺满整行、无空隙，Google Photos 风格）。库媒体不显示文件名，缩略图无卡片背景且不裁剪（网格正切为裁剪显示）；图片保持纯净展示，视频叠加播放标识、清晰度和时长。
- “视频”和“照片”功能跨全部正式收藏库分页展示，不再按目录分组，按 `media.mtime` 倒序，每页网格自适应并尽量占满可用空间。页面顶部显示设置条提供布局切换（网格自适应 / 网格正切 / 瀑布流 / 自适应）、缩放滑杆和每页数量（10/20/50/100）设置；底部为紧凑分页栏（页码输入跳转、上一页/下一页）。缩略图无卡片背景，文件名显示在缩略图下方；视频额外显示播放标识、清晰度和时长。
- 视频清晰度按高度分级：低于 720 显示实际高度（如 480P），720–1079 显示 720P，1080–1439 显示 1080P，1440–2159 显示 2K，2160 及以上显示 4K。
- 库/视频/照片的缩放、布局与每页数量设置均为客户端本地显示偏好（`ui.home.library_thumb_extent`、`ui.home.*_layout`、`ui.home.*_page_size` 等），不写入服务端数据库；切换布局时内容淡入过渡，网格悬停有放大反馈。
- 瀑布流布局由客户端自实现 sliver（`widgets/masonry_sliver.dart`，基于 `SliverMultiBoxAdaptorWidget` 手写分列布局，item 高度按列宽 × 宽高比精确计算）；不使用 flutter_staggered_grid_view（其 SliverMasonryGrid 在内容追加时可能通过 scrollOffsetCorrection 把滚动位置拉回顶部）。追加内容（触底加载）不影响滚动位置，已由回归测试覆盖。
- “统计”功能展示图片大小与数量、视频大小与数量、视频总时长和收藏库总大小，并使用大数字指标、占比进度条与空间占比环形图（Donut）的现代化仪表盘布局，支持刷新。
- 首页四个功能视图常驻（IndexedStack），切换功能不丢失滚动位置、页码与已加载数据。
- 媒体网格采用相册式交互：缩略图加载中显示脉动骨架占位、就绪后自然覆盖；鼠标悬停时卡片轻微放大并浮出右上角快捷操作（打开媒体 / 更多操作）；首屏数据加载时展示骨架网格而非居中转圈。
- 媒体右键菜单包含：打开媒体（应用内查看器）、打开文件、打开所在目录、复制文件路径、删除此文件（确认后源文件移入回收站）；删除成功后当前列表就地移除。
- 空状态升级为图标 + 引导文案：库功能空状态提供“前往扫描”按钮直达扫描页；视频/照片空状态显示对应类型引导文案。
- 视频/照片翻页时内容淡入过渡；库布局切换时页面淡入过渡。
- 滑动体验优化：桌面端网格与库页显示细滚动条，库页触底加载分组时显示骨架行，媒体单元以 RepaintBoundary 隔离避免滚动重绘。
- 首页打开媒体时，查看器上一项/下一项保持当前首页目录组或当前分页的媒体队列；收藏库管理页仍使用所在目录媒体队列。
- 首页分组层级、库/视频/照片布局与缩放、库分组页大小与加载方式、视频/照片页大小均为客户端本地显示偏好，不写入服务端数据库。
- 库页与视频/照片页的公共组件（`MediaTile`/`DisplayToolbar`/`PaginationBar` 等）与工具函数集中在 `widgets/media_common.dart`；库页主体（翻页/懒加载/搜索）在 `screens/library_tab.dart`。

### 7.6.4 设置页存储位置

- 设置页展示服务端保存位置：图片缩略图目录、视频封面目录、日志位置（通过 `GET /api/settings` 获取，路径均为服务端绝对路径）。
- 日志默认输出到控制台（标准输出）；配置文件 `log.file` 非空时将日志追加写入该文件，设置页相应显示文件路径。

### 7.6.5 时间显示

- 后端统一存储/返回 UTC 时间；客户端展示时统一转换为本地时区（任务创建/开始/完成时间、文件统计时间等）。

- 三栏自适应布局：左栏收藏库 + 目录树，中间媒体网格，右栏详情/检查面板；面板可折叠、可调宽，窗口与面板状态持久化。
- 报告页用分段切换（目录树视图 / 分组视图）+ 筛选 chips（全部 / 图片 / 视频）+ 结果内搜索框。

### 7.6.4 媒体卡片与交互

- 统一缩略图卡片：hover 浮出操作条（打开 / 复制路径 / 删除）、多选复选框、骨架屏占位；卡片显示文件名、文件大小、图片分辨率或视频时长；视频卡片叠加播放图标与时长徽标。
- card_swiper 使用叠卡实现：默认错位堆叠露出边缘，hover 展开，点击放大展示全部，弹性动画过渡。
- 桌面式右键菜单：图标 + 文字 + 分隔线 + 二级菜单；长按作为触屏回退。
- 快捷键：Ctrl+F 搜索、Enter 打开、Ctrl+C 复制路径、Delete 删除、Ctrl+A 全选。
- 应用内查看器（双击打开媒体）支持视频倍速播放（0.5x–2x）与画面旋转（0°/90°/180°/270°，基于 mpv `video-rotate`，桌面端生效；切换视频后旋转复位）。
- 图片专用查看器（`ImageViewer`，入口按媒体类型分流：图片走此查看器，视频走视频查看器）：全屏黑底、不显示标题与文件名；左侧展示本目录全部图片的正方形缩略图条（当前项红色高亮，点击切换，可一键隐藏/显示，隐藏后图片区占满全宽）；中央图片区支持滚轮/按钮/键盘缩放（↑ 放大、↓ 缩小，1x–10x）与平移、旋转（0°/90°/180°/270°）、一键复原缩放（回到 100% 居中，旋转不变）；放大超出屏幕时右上角显示鸟瞰图（当前视口位置红色框，随缩放/平移实时更新，详情面板打开时隐藏）；底部功能栏（上一个/下一个/缩放/复原/旋转/列表显隐/详情/全屏/关闭）仅在鼠标移至屏幕正下方区域时显示（打开查看器短暂显示 2 秒引导）；键盘 ←/→ 切换上一张/下一张图片（本目录内循环）、Esc 关闭；切换图片时缩放与旋转全部重置；"详情"按钮在右侧滑出完整元数据面板（类型/格式/分辨率/大小/修改时间/完整相对路径/SHA1/pHash）；"全屏"按钮桌面端切换应用窗口全屏、Web 端切换浏览器全屏（Web 退出全屏带非全屏守卫，防止 "Document not active" 报错）。

### 7.6.5 反馈、空状态与性能

- 报告顶部摘要卡展示重复组数、文件数、可释放空间与类型占比。
- 操作完成用 Snackbar 反馈；空状态用插画 + 引导文案；后台任务进度持久化（进度条 + ETA + 可取消）。
- 缩略图缓存 + 虚拟化网格，窗口缩放自适应列数；视图切换过渡动画与 hover 微交互。
- 可访问性：焦点环、键盘可达、tooltip、高对比度模式。

------

# 8. 数据库设计要点

- 表结构脚本：`schema.sql`（SQLite，直接执行即可初始化）。
- `media` 表统一存图片与视频，`kind` 区分；便于路径迁移与统一查询。
- `media.relative_path` 存相对收藏库根目录的路径，根目录迁移仅 UPDATE `libraries.path`；`media.thumbnail_path` 存相对"该类型缩略图根目录"的内容寻址路径（图片/视频各自根目录），缩略图目录整体移动后仅需更新配置，无需改库。
- 缩略图根目录默认取系统数据目录（Windows `%LOCALAPPDATA%`、macOS `~/Library/Application Support`、Linux `$XDG_DATA_HOME`/`~/.local/share` 下的 `memable/thumbnails/{image,video}`）；配置 `thumbnail.image_dir`/`video_dir` 后以配置为准。
- 同步扫描以 `(library_id, relative_path)` 匹配本地文件与 `media` 记录；仅当 `mtime`、`file_size` 一致，且处理状态、必要字段和持久化缩略图文件均完整时才跳过。
- 仅在收藏库完整遍历成功后清理本地已不存在的 `media` 记录、持久化缩略图和失效关联结果；收藏库不可访问、扫描取消或遍历出错时不得执行缺失文件清理。
- 同步扫描支持强制重新处理全部文件，用于数据修复或算法升级后的重新计算。
- 临时扫描：`scan_sessions.is_temporary=1` 标记，不另建物理临时表；入库时 UPDATE 标记并移动文件。
- 扫描 worker 并发完成媒体采集后，媒体记录按 200 条一批在单事务中写入 SQLite，避免每条记录单独提交事务；数据库仍保持单写连接以符合 SQLite 约束。
- 视频只持久化一张封面图，路径由 `media.thumbnail_path` 保存；封面截取规则见 5.3。
- `media.phash` 对图片存图片 pHash，对视频存 Stash 风格 25 截图 sprite pHash；计算 sprite pHash 的截图和 sprite 图不入库、不持久化。
- pHash/dHash/aHash/oshash 均以 16 进制字符串存储，便于跨语言/跨平台一致；oshash 兼容 Stash/OpenSubtitles OSHash。
- 阈值（视频 sprite pHash Hamming 距离、允许时长差等）、缩略图目录/最大边等运行参数**由代码中的配置文件提供**（如 config.yaml），不入库。
- 重复报告数据使用三张独立表存储：`duplicate_reports`、`duplicate_groups`、`duplicate_group_members`；启用 `PRAGMA foreign_keys=ON`，成员只存 `media_id` 引用、不冗余路径，目录/完整路径/缩略图通过 JOIN `media` 实时取得。
- `background_tasks.result_json` 不再存放重复报告分组明细，仅保留任务进度与统计摘要（组数、文件数、各类型计数）。

### 重复报告表结构

`duplicate_reports`（报告主表）：

| 字段 | 说明 |
| --- | --- |
| id | 主键 |
| background_task_id | 关联后台任务 |
| scope | all（全部数据）/ same_dir（仅同一目录） |
| media_type | image / video / all |
| image_threshold | 图片相似度阈值 0–100 |
| video_phash_threshold | 视频 sprite pHash Hamming 距离阈值 |
| video_duration_diff | 允许时长差（较大时长的百分比） |
| oshash_filter | 是否启用 oshash 粗筛 |
| include_sha1 | 是否包含 SHA1 完全相同结果 |
| stale | 1=数据已变化需重新生成 |
| total_groups / total_files | 统计快照（组数、文件数） |
| created_at | 生成时间 |

`duplicate_groups`（重复组）：

| 字段 | 说明 |
| --- | --- |
| id | 主键 |
| report_id | 外键 → duplicate_reports.id，ON DELETE CASCADE |
| group_type | sha1 / image_similar / video_similar |

`duplicate_group_members`（组成员）：

| 字段 | 说明 |
| --- | --- |
| id | 主键 |
| group_id | 外键 → duplicate_groups.id，ON DELETE CASCADE |
| media_id | 外键 → media.id，ON DELETE CASCADE，建索引 |
| UNIQUE(group_id, media_id) | 防止同一媒体重复加入同一组 |

------

# 9. 工具 - 文件统计与目录差异

## 9.1 文件统计

对指定目录递归统计文件数量、总大小、扩展名分布（数量/空间占比）与文件树，记录保存到 `file_stats` 表（`dir_path`、`total_bytes`、`total_count`、`ext_stats` JSON、`file_tree` JSON），支持历史记录列表/详情查看/删除。

## 9.2 目录差异（diff）

- 历史统计记录的每一行提供"对比新增/删除文件"按钮：服务端按该记录保存的 `dir_path` **重新遍历当前目录**，与历史 `file_tree` 中的文件集合对比：
  - **新增文件** = 当前存在但历史不存在；**删除文件** = 历史存在但当前不存在；
  - 相对路径以正斜杠表示，结果按字典序排序返回。
- 统计目录不存在或不可访问时返回错误提示，不进行对比。
- 差异结果以两个**目录树**展示（新增=绿色、删除=红色），树层级完整（目录 → 子目录 → 文件叶子，叶子显示文件名），**默认全部折叠**，点击目录行展开/收起。
- 提供"导出Excel"：生成 .xlsx（服务端用 Go 标准库 `archive/zip` 手写，无第三方依赖），包含两个 sheet——**新增文件列表**、**删除文件列表**，列名均为"文件路径"，写入**完整绝对路径**（正斜杠），按字典序逐行写入；客户端通过文件保存对话框导出。

## 9.3 接口

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | /api/tools/file-stats | 创建文件统计 |
| GET | /api/tools/file-stats | 统计记录列表 |
| GET | /api/tools/file-stats/{id} | 单条统计详情 |
| POST | /api/tools/file-stats/{id}/diff | 目录差异：返回 `{dir_path, added[], removed[], added_count, removed_count}`（相对路径） |
| POST | /api/tools/file-stats/{id}/diff/export | 导出 xlsx（两 sheet：新增/删除文件列表，绝对路径） |
| DELETE | /api/tools/file-stats/{id} | 删除统计记录 |
