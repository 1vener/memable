# 本地媒体相似度/重复检测管理系统

## 产品需求文档（PRD）

版本：

```
v1.0
```

------

# 1. 项目概述

## 1.1 项目目标

开发一个运行在本地的视频和图片重复检测管理软件。

代码注释使用中文

支持：

- 本地媒体库管理
- 图片/视频扫描
- 媒体元数据采集
- 图片重复检测
- 视频重复检测
- 相似媒体分析
- 重复报告生成
- 文件整理归档

特点：

- 完全本地运行
- 不依赖云服务
- 支持大规模媒体库
- 支持后续AI扩展

------

# 2. 技术方案

## 技术栈

| 模块     | 技术           |
| -------- | -------------- |
| 客户端   | Flutter        |
| 服务端   | Go             |
| 数据库   | SQLite         |
| 视频解析 | ffprobe        |
| 视频处理 | ffmpeg         |
| 图片处理 | Go image       |
| 任务调度 | Go Worker Pool |

------

# 3. 功能需求

# 3.1 收藏库管理

用户可以维护媒体收藏目录。

例如：

```
照片库

D:/Pictures


视频库

E:/Videos
```

支持：

- 添加目录
- 删除目录
- 修改目录
- 重新扫描

删除收藏库时，必须在同一数据库事务中删除收藏库记录、关联媒体记录和扫描会话；同时删除该库生成且已无其他收藏库引用的缩略图文件。不得删除收藏库目录中的源媒体文件。

路径单独管理。

收藏库的 `path` 字段存储根目录绝对路径；库内所有媒体/缩略图均存储**相对该根目录的路径**（UTF-8 规范化）。

原因：

例如：

旧：

```
D:/Media
```

新：

```
E:/Media
```

只需 UPDATE `libraries.path` 一处即可，无需重新扫描、无需更新任何子文件记录；展示和搜索时拼接 `library.path || relative_path` 得到完整路径。

- 收藏库扫描后支持展示文件树，当删除文件或目录时同时删除数据库里相关数据和生成的缩略图/视频帧（后文会提到）

------

# 3.2 媒体扫描

扫描流程：

```
扫描目录

↓

发现文件

↓

判断数据库中该文件是否已经扫描处理过：以 `(library_id, relative_path, mtime, file_size)` 联合判定。只有路径、修改时间、文件大小三者均一致时才跳过；任一变化即视为修改过的文件，需重新采集 metadata/hash/缩略图（视频需重新生成封面图）

↓

判断类型并采集metadata

↓

生成hash

↓

生成缩略图

↓

保存数据库
```

------

# 4. 图片处理需求

## 4.1 图片metadata

采集：

| 字段     |
| -------- |
| 文件大小 |
| 格式     |
| 宽度     |
| 高度     |
| 创建时间 |
| 修改时间 |
| 文件路径 |

------

## 4.2 图片hash

生成：

```
SHA1
```

用途：

完全相同文件检测。

生成：

```
pHash
dHash
aHash
```

用途：

相似图片检测。

------

## 4.3 图片缩略图

生成：

```
最大边300px
```

保存：

```
例子：thumbnail/image/
路径可在配置文件中配置
```

------

# 5. 视频处理需求

# 5.1 视频metadata

采集：

| 字段        |
| ----------- |
| size        |
| duration    |
| video_codec |
| audio_codec |
| format      |
| width       |
| height      |
| frame_rate  |
| bit_rate    |
| 文件路径    |

------

# 5.2 视频SHA1

生成：

```
SHA1
```

用途：

检测：

完全相同视频。

例如：

```
movie.mp4

movie_copy.mp4
```

即使：

文件名不同

SHA1一致。

------

# 5.3 视频封面图缩略图

视频只持久化一张封面图缩略图。用于 sprite pHash 的 25 张截图仅在计算过程中临时生成，计算完成后立即删除。

## 封面截取时间点

先通过 `ffprobe` 获取视频 duration，再按以下规则选择时间点：

```
duration < 10s       -> 50% duration
10s <= duration < 60s -> 30% duration
duration >= 60s      -> 10% duration
```

如果选定时间点抽帧失败、画面为黑屏或接近纯色，则在该时间点前后按 10% duration 间隔重试，最多尝试 5 个时间点；仍失败时使用 50% duration，最后才回退到 0s。

封面生成流程：

```
ffprobe 获取 duration

↓

计算封面时间点

↓

ffmpeg 抽取单张图片

↓

过滤黑屏/近纯色并 resize

↓

保存到配置的 video thumbnail 目录
```

缩略图最大边为 300px，生成路径登记到 `media.thumbnail_path`。

------

# 5.4 视频相似度检测

## 第一层

SHA1：

```
完全重复
```

结果：

100%重复。

------

## 第二步：oshash 粗筛

使用 Stash/OpenSubtitles OSHash（字段名统一使用 `oshash`）做文件级快速指纹粗筛。

说明：

- oshash 相同可快速定位相同或高度可疑候选。
- oshash 是文件级快速指纹，不是视觉相似哈希，不能单独判定转码、裁剪、加水印等视觉相似视频。
- oshash 不同的视频也可以进入后续视觉相似检测，避免漏掉转码后视觉相似但文件头尾不同的内容。

------

## 第三步：视觉相似检测

参考 Stash `videophash` 流程，为每个视频生成一个整体视觉 sprite pHash：

```
在 duration 的 5% 到 95% 区间均匀抽取 25 张截图

↓

每张截图缩放为宽 160px

↓

按 5 × 5 拼成 sprite 图

↓

对 sprite 图生成 64 bit pHash
```

截图时间点：

```
chunk_count = 25
offset = duration * 0.05
step = duration * 0.90 / chunk_count
time_i = offset + i * step, i = 0..24
```

这 25 张截图和 sprite 图只作为计算过程中的临时文件或内存数据，计算完成后立即删除，也不作为缩略图保存。视频持久化图片仍只有一张封面图。

视频 sprite pHash 保存到：

```
media.phash
```

比较两个视频的 sprite pHash：

```
video_phash_distance = Hamming(media_a.phash, media_b.phash)
```

同时使用视频时长差作为辅助条件：

```
duration_diff = abs(duration_a - duration_b)
```

只有 `video_phash_distance` 和 `duration_diff` 均满足配置文件阈值时，才判定为可能视觉相似。

命中的相邻视频按连通关系合并为重复/相似分组，而不是只输出两两结果。

------

# 5.5 OSHash

参考：

Stash项目的 OpenSubtitles OSHash 实现。

算法：

```
result = file_size
       + uint64_le_sum(first 64KiB)
       + uint64_le_sum(last 64KiB)
```

输出：

```
16位十六进制字符串
```

说明：

该 oshash 是文件级快速指纹，不是视觉相似哈希；用于粗筛候选并兼容 Stash/OpenSubtitles 生态。视觉相似以第三步的 sprite pHash 为准。

用途：

视频整体指纹。

保存：

```
media.oshash
```

用于：

快速过滤候选视频。

检测流程：

```
oshash粗筛

↓

Stash风格视频sprite pHash候选分组
```



# 6.搜索

## 6.1搜索框

> - 搜索框支持输入文字搜索，搜索类型包含文件名（可以模糊匹配全路径）,sha1。
>
> 搜索结果显示文件全路径、图片缩略图或视频封面图等
>
> - 搜索框支持图片搜索，对比图片缩略图和视频封面图



## 6.2 选择本地文件目录，参照3.2媒体扫描，但结果写入同一套表，用 `scan_sessions.is_temporary=1` 标记为临时扫描，并关联一个唯一的 `scan_session_id`。图片缩略图和视频封面图放入临时目录；视频 sprite pHash 的过程截图和 sprite 图仅临时使用，计算后立即删除。

采用"同表 + 临时标记"而非物理临时表，原因：
- 避免临时表与正式表之间 JOIN 不可见导致的漏检
- 用户确认入库时只需 `UPDATE scan_sessions SET is_temporary=0` 并移动文件，无需跨表复制记录

扫描完进行相似检测，生成重复报告（html格式，需要关联源文件，要显示文件全路径、图片缩略图或视频封面图等）

搜索任务结束，用户可以选择是否入收藏库：选择"是"时，将文件**移动**到收藏库指定目录（不要复制），然后将对应 `scan_session_id` 下记录 `is_temporary` 置 0，临时目录的图片缩略图和视频封面图移动到配置的缩略图目录。

# 7. 重复报告

扫描本地收藏库的重复数据，图片和视频的分开生成，用户可选择单独进行图片重复报告生成或视频重复报告生成

------

# 8. 数据库设计要点

- 表结构脚本：`schema.sql`（SQLite，直接执行即可初始化）。
- `media` 表统一存图片与视频，`kind` 区分；便于路径迁移与统一查询。
- 所有媒体和持久化缩略图均存**相对收藏库根目录的路径**；根目录迁移仅 UPDATE `libraries.path`。
- 重复扫描判定：`(library_id, relative_path, mtime, file_size)` 三者一致才跳过。
- 临时扫描：`scan_sessions.is_temporary=1` 标记，不另建物理临时表；入库时 UPDATE 标记并移动文件。
- 视频只持久化一张封面图，路径由 `media.thumbnail_path` 保存；封面截取规则见 5.3。
- `media.phash` 对图片存图片 pHash，对视频存 Stash 风格 25 截图 sprite pHash；计算 sprite pHash 的截图和 sprite 图不入库、不持久化。
- pHash/dHash/aHash/oshash 均以 16 进制字符串存储，便于跨语言/跨平台一致；oshash 兼容 Stash/OpenSubtitles OSHash。
- 阈值（视频 sprite pHash Hamming 距离、允许时长差等）、缩略图目录/最大边等运行参数**由代码中的配置文件提供**（如 config.yaml），不入库。


