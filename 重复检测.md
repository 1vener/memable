图片/视频重复对比逻辑（现状）
一、整体流程
报告页"生成重复报告" → POST /api/reports/duplicate
→ 入队 report_duplicate 任务（独立报告队列）
→ runner.execDuplicateReport → duplicate.Service.Generate
→ NewDetector(MediaRepo, Config) + DetectWithOptions(Options)
→ 结果按组写入三张表（ReplaceReport 单事务清旧写新）
- 数据源：MediaRepo.ListAllFormal() / ListByKind(kind)——只取正式媒体（is_temporary=0，排除临时扫描结果），按 library_id, relative_path 排序；检测全部基于入库时已算好的指纹（phash/sha1/oshash/duration_ms），不重新解码文件。
  二、检测选项（Options）
  选项	默认
  scope	all
  media_type	all
  image_threshold	90（配置换算）
  video_phash_distance	12（配置）
  video_duration_diff_ms	3000（配置）
  oshash_filter	true
  include_sha1	true
  三、图片检测（detectImages）
  第一层：SHA1 精确重复（include_sha1=true 时）
- groupBySha1：sha1 非空才参与，按 SHA1 分组，组员 ≥2 → sha1_exact 组
- 这些媒体被标记 represented，不再进入第二层（避免同文件重复出现在多个组）
  第二层：pHash 相似
- 候选 = 未进 SHA1 组 且 phash 非空的图片
- detectImagePHashSimilarDist：O(n²) 全两两比较 Hamming 距离 ≤ maxDist → 并查集 union → 连通分量 ≥2 → phash_similar 组
- 注意：dHash/aHash 字段虽存库，实际不参与比较，只用 pHash
  四、视频检测（detectVideos）
  第一层：SHA1 精确（同图片，视频需先经 scan_sha1 任务补齐 SHA1 才可能命中）
  第二层：sprite pHash 相似
- 候选 = 未进 SHA1 组 且 phash 非空
- detectVideoPHashSimilarDist：O(n²) 两两比较，需同时满足：
1. sprite pHash Hamming 距离 ≤ video_phash_distance
2. 时长差（毫秒绝对值）≤ video_duration_diff_ms
- 并查集合并 → 连通分量 ≥2 → sprite_phash_similar 组
- oshash_filter 参数被 _ = oshashFilter 忽略——oshash 仅存库作候选提示，不得作为硬过滤（否则视觉相似但 oshash 不同的视频漏检）
  五、scope=same_dir 模式
- 按 relDir(relative_path) 分桶，各桶内独立执行上述检测
- 再经 filterSameDir：只保留组内全部成员位于同一目录（不含子目录）的组
  六、结果映射与持久化
  reason
  sha1_exact
  phash_similar
  sprite_phash_similar
  Service.Generate → ReplaceReport：单事务删除旧报告（组/成员级联）→ 写入新报告 → 刷新统计。
  七、关键特征/潜在问题
1. O(n²) 全两两比较：图片/视频量大时检测耗时显著（无分桶/索引加速，仅 same_dir 模式按目录缩小范围）
2. pHash 比较在内存进行（全部媒体一次载入），海量库可能占内存高
3. SHA1 组与相似组互斥（represented 排除），但同一媒体可同时出现在相似组内（并查集保证每组独立）
4. 无 sha1/phash 指纹的媒体（如扫描失败、GIF 跳过）不参与检测
5. 视频只有先跑 scan_sha1 补齐 SHA1 才能进入精确重复层
   如需后续优化（如索引加速、分桶比较、并发检测），可以在此基础上规划。